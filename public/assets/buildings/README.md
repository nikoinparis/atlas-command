# Building Assets

Place final transparent PNG or WebP isometric building sprites here.

Use the filenames documented in `docs/ART_PIPELINE.md`, such as `hq.png`,
`treasury.png`, and `content-studio.png`. Building click zones, label offsets,
shadow sizes, and sprite dimensions are configured in `lib/game/buildingAssets.ts`.
